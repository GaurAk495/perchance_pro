{
  "name": "ext",
  "module": "dist/index.js",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.2.2"
  },
  "scripts": {
    "build": "bun build src/content/index.ts --outdir dist && bun build src/background/background.ts --outdir dist && bun build src/sidebar/sidebar.ts --outdir dist && bun run copy-assets.ts"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
